{
  "buildCommand": "cd TRACEXBYCA/frontend && npm install && npm run build",
  "outputDirectory": "TRACEXBYCA/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
